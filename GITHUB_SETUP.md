# 📚 Простая инструкция: Как сохранять изменения в GitHub

## Шаг 1: Создайте репозиторий на GitHub

1. Откройте https://github.com/dzakharenkov
2. Нажмите зелёную кнопку **"New"** (создать новый репозиторий)
3. Название репозитория: `Sales` (или любое другое)
4. **НЕ ставьте галочки** (не создавайте README, .gitignore, лицензию)
5. Нажмите **"Create repository"**

## Шаг 2: Настройте Git в проекте (один раз)

Откройте PowerShell в папке проекта и выполните команды:

```powershell
cd d:\Python\Sales

# 1. Инициализация git
git init

# 2. Настройка имени и email (если ещё не настроено)
git config user.name "dzakharenkov"
git config user.email "ваш_email@example.com"

# 3. Добавить все файлы
git add .

# 4. Создать первый коммит
git commit -m "Первый коммит: базовая версия SDS"

# 5. Подключить к GitHub (замените URL на ваш!)
git remote add origin https://github.com/dzakharenkov/Sales.git

# 6. Отправить на GitHub
git branch -M main
git push -u origin main
```

**Важно:** При первом `git push` GitHub попросит авторизоваться:
- Откроется браузер
- Войдите в свой аккаунт GitHub
- Разрешите доступ

## Шаг 3: Сохранение изменений (каждый раз после правок)

Когда вы что-то изменили в проекте, выполните:

```powershell
cd d:\Python\Sales

# 1. Посмотреть, что изменилось (необязательно)
git status

# 2. Добавить все изменения
git add .

# 3. Создать коммит с описанием
git commit -m "Описание ваших изменений"

# 4. Отправить на GitHub
git push
```

### Примеры описаний коммитов:
- `"Исправлены синтаксические ошибки в app.js"`
- `"Добавлен favicon"`
- `"Обновлена документация"`
- `"Исправлена ошибка авторизации"`

## Полезные команды

```powershell
# Посмотреть историю коммитов
git log

# Посмотреть, что изменилось
git status

# Отменить изменения в файле (если ещё не закоммитили)
git checkout -- имя_файла.py

# Посмотреть разницу
git diff
```

## Если что-то пошло не так

```powershell
# Удалить неправильный коммит (если ещё не отправили на GitHub)
git reset --soft HEAD~1

# Если нужно изменить последний коммит
git commit --amend -m "Новое описание"
```

---

**Готово!** Теперь все ваши изменения будут сохраняться на GitHub. 🎉
