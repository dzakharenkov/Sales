# Быстрый запуск проекта после перезагрузки ПК

## 🚀 Простой способ (рекомендуется)

1. **Откройте папку проекта** в проводнике: `d:\Python\Sales`

2. **Дважды кликните** на файл `run.bat`

3. **Откройте браузер** и перейдите по адресу: http://127.0.0.1:8000

4. **Войдите в систему:**
   - Логин: `dzakharenkov`
   - Пароль: `admin123`

---

## 📋 Альтернативный способ (через командную строку)

1. Откройте PowerShell или CMD

2. Перейдите в папку проекта:
   ```powershell
   cd d:\Python\Sales
   ```

3. Запустите сервер:
   ```powershell
   python -m uvicorn src.main:app --host 0.0.0.0 --port 8000 --reload
   ```

4. Откройте браузер: http://127.0.0.1:8000

---

## ⚙️ Если что-то не работает

### Проблема: Python не найден
**Решение:** Убедитесь, что Python установлен и добавлен в PATH. Проверьте:
```powershell
python --version
```

### Проблема: Ошибки при установке пакетов
**Решение:** Установите зависимости:
```powershell
pip install -r requirements.txt
```

Если появляется ошибка про Rust/pydantic-core:
```powershell
pip install --only-binary :all: -r requirements.txt
```

### Проблема: Ошибка подключения к базе данных
**Решение:** 
- Проверьте интернет-соединение (БД на удаленном сервере)
- Убедитесь, что файл `.env` существует (скопируйте из `.env.example`)

---

## 📝 Полезные ссылки после запуска

- **Главная страница:** http://127.0.0.1:8000
- **Страница входа:** http://127.0.0.1:8000/login
- **Документация API (Swagger):** http://127.0.0.1:8000/docs
- **ReDoc:** http://127.0.0.1:8000/redoc
- **Проверка здоровья:** http://127.0.0.1:8000/health

---

## 💡 Совет

Создайте ярлык на рабочем столе для файла `run.bat` — тогда запуск будет еще проще!
