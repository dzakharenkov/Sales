# Инструкция по деплою Telegram-бота

## Исправления в версии 2.3

### Исправление #28: Создание клиента — Доступность всех полей
**Проблема:** После ввода ИНН поля для заполнения перестают появляться.

**Что исправлено:**
- Добавлена кнопка "Назад к меню полей" при редактировании каждого поля
- Исправлена очистка состояний в `_clear_agent_state` (добавлены флаги `create_visit_*`)
- Добавлено подробное логирование для отладки

**Проверка:**
1. Нажмите "Добавить клиента"
2. Введите название клиента
3. Введите ИНН (9-12 цифр) или нажмите "Пропустить"
4. **Должно появиться меню полей с галочками** для: Название, ИНН, Название фирмы, Р/с, Адрес, Город, Территория, Телефон, Контактное лицо, Координаты, Фото
5. Нажмите на любое поле для редактирования
6. Теперь есть кнопка "◀️ Назад к меню полей" для возврата без ввода значения

### Исправление #32: Создать визит — Поиск клиента
**Проблема:** После ввода клиента не выводятся клиенты, на которых будет создан визит.

**Что исправлено:**
- Исправлена очистка состояний (добавлены флаги `create_visit_search`, `create_visit_customer_id`, и т.д.)
- Добавлено подробное логирование поиска клиентов
- Улучшена обработка ошибок API

**Проверка:**
1. Нажмите "Создать визит"
2. Введите название клиента или ИНН
3. **Должен появиться список клиентов** с кнопками для выбора
4. Выберите клиента
5. Введите дату визита (ДД.ММ.ГГГГ)
6. Введите время визита (ЧЧ:ММ) или /skip
7. Подтвердите создание визита

---

## Деплой на сервер Ubuntu

### Шаг 1: Деплой файлов с Windows на Ubuntu
```cmd
deploy_to_ubuntu.bat
```

Скрипт скопирует на сервер:
- `src/` — весь код приложения и бота
- `migrations/` — миграции БД
- `requirements.txt` — зависимости Python
- `.env` — конфигурация (токен бота, API URL, БД)
- `sales_sql.sql` — SQL-скрипты

**ВАЖНО:** Скрипты `ubuntu_*` НЕ копируются автоматически (из-за проблем с кодировкой Windows→Linux).
Создайте их **ОДИН РАЗ** на сервере, следуя инструкции в файле `UBUNTU_SCRIPTS_SETUP.txt`.

### Шаг 2: Подключение к серверу Ubuntu
```bash
ssh dima@45.141.76.83
cd /var/www/sales.zakharenkov.ru/html
```

### Шаг 3: Установка зависимостей (первый раз или после обновления requirements.txt)
```bash
# Создать виртуальное окружение (если еще нет)
python3 -m venv venv

# Активировать venv
source venv/bin/activate

# Установить зависимости
pip install -r requirements.txt
```

### Шаг 4: Перезапуск бота
```bash
# ВАЖНО: Всегда перезапускайте бота после деплоя!
./ubuntu_restart_telegram_uz_sales_bot
```

Или вручную:
```bash
# Остановить бота
./ubuntu_stop_telegram_uz_sales_bot

# Подождать 2 секунды
sleep 2

# Запустить бота
./ubuntu_telegram_uz_sales_bot
```

### Шаг 5: Проверка логов
```bash
# Просмотр последних 50 строк лога
tail -50 telegram_bot.log

# Просмотр лога в реальном времени
tail -f telegram_bot.log

# Поиск ошибок в логах
grep -i "error\|exception\|traceback" telegram_bot.log

# Просмотр логов для конкретного пользователя (замените 123456 на Telegram user ID)
grep "User 123456" telegram_bot.log
```

### Полезные команды

**Проверка статуса бота:**
```bash
# Проверить, запущен ли бот
cat telegram_bot.pid && echo "Bot is running" || echo "Bot is not running"

# Проверить процесс
ps aux | grep telegram_bot
```

**Очистка старых логов:**
```bash
# Очистить лог-файл
> telegram_bot.log

# Или архивировать старый лог
mv telegram_bot.log telegram_bot_$(date +%Y%m%d_%H%M%S).log
```

**Проверка конфигурации:**
```bash
# Проверить .env файл
cat .env

# Проверить токен бота (первые 10 символов)
grep TELEGRAM_BOT_TOKEN .env | cut -c1-30
```

---

## Отладка проблем

### Проблема: Бот не запускается
**Решение:**
1. Проверьте логи: `tail -50 telegram_bot.log`
2. Проверьте токен бота в `.env`: `grep TELEGRAM_BOT_TOKEN .env`
3. Проверьте подключение к БД: `grep DATABASE .env`
4. Попробуйте запустить вручную: `python3 -m src.telegram_bot.bot`

### Проблема: "Invalid URL" или ошибки с '\r'
**Решение:** CRLF line endings в .env файле (Windows → Linux)
- Уже исправлено в `config.py` добавлением `.strip()`
- Если все еще есть проблема: `dos2unix .env` на сервере

### Проблема: "Сессия истекла" при каждом запросе
**Решение:** Проверьте подключение к БД
```bash
# На сервере Ubuntu
psql -h 45.141.76.83 -p 5433 -U postgres -d localdb -c "SELECT COUNT(*) FROM telegram_bot_sessions;"
```

### Проблема: Меню полей (#28) или список клиентов (#32) не появляются
**Решение:**
1. Проверьте логи для вашего Telegram user ID:
   ```bash
   grep "User YOUR_TELEGRAM_ID" telegram_bot.log | tail -20
   ```
2. Должны быть логи:
   - `INN validated, showing fields menu` (для #28)
   - `Handling create visit search` и `Found N customers` (для #32)
3. Если логов нет — бот не обновлен, перезапустите: `./ubuntu_restart_telegram_uz_sales_bot`

---

## Контрольный список после деплоя

- [ ] Файлы скопированы на сервер (`deploy_to_ubuntu.bat`)
- [ ] Подключились к серверу Ubuntu (`ssh dima@45.141.76.83`)
- [ ] Перешли в директорию (`cd /var/www/sales.zakharenkov.ru/html`)
- [ ] Перезапустили бота (`./ubuntu_restart_telegram_uz_sales_bot`)
- [ ] Проверили логи (`tail -50 telegram_bot.log`)
- [ ] Протестировали функционал в Telegram:
  - [ ] Добавление клиента (ввод названия → ИНН → меню полей)
  - [ ] Создание визита (поиск клиента → список клиентов → выбор → дата/время)
  - [ ] Загрузка фото клиента
  - [ ] Создание заказа

---

## Архитектура бота

```
src/telegram_bot/
├── bot.py                    # Точка входа, регистрация обработчиков
├── config.py                 # Конфигурация (токен, API URL, таймауты)
├── sds_api.py                # HTTP-клиент для SDS API
├── session.py                # Управление сессиями (asyncpg → БД)
├── handlers_auth.py          # Авторизация (/start, login/password)
├── handlers_agent.py         # Функционал агента (клиенты, визиты, заказы, фото)
├── handlers_expeditor.py     # Функционал экспедитора (маршруты, доставка, оплата)
└── helpers.py                # Утилиты (форматирование, кэш справочников)
```

**Важно:**
- Бот использует **прямое подключение к БД** (asyncpg) для сессий и логов
- Все бизнес-логика выполняется через **SDS API** (`src/api/`)
- Логи пишутся в `telegram_bot.log` (уровень INFO)

---

## История изменений

### v2.3 (2026-02-16)
- ✅ Исправление #28: Меню полей после ввода ИНН
- ✅ Исправление #32: Список клиентов при создании визита
- ✅ Добавлена кнопка "Назад к меню полей"
- ✅ Улучшено логирование для отладки
- ✅ Исправлена очистка состояний в `_clear_agent_state`

### v2.2 (предыдущая)
- Исправление #30: Кнопка "Доставлен" обновляет статус заказа
- Исправление #33: Кнопка "Полученная оплата" для экспедиторов
- Исправление CRLF line endings (.strip() в config.py)
- Исправление #27: Поиск клиентов по ИНН (параметр search)

---

Удачи! 🚀
